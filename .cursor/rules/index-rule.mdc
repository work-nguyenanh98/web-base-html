---
description: 
globs: index
alwaysApply: false
---
[index.html](mdc:index.html)

When updating the homepage with content, follow these guidelines:

- **Title + Description SEO**: Update the `<title>`, `<meta name="description">`, and `<meta name="keywords">` tags in the `<head>` to match the provided brand, description, and keywords. Keywords should be generated based on the title and description if not explicitly provided.
- **Link web **: Update all link website
- **Schema**: Replace or update the `<script type="application/ld+json">` block with the provided JSON-LD schema, ensuring it matches the current brand, address, menu, cuisine, and hours.
- **Brand Name**: Update all visible brand name text (e.g., in the navbar) to the new brand.
- **Our Story**: Update the "Our Story" section content to reflect the new brand's story, if provided.
- **Highlights at Our Restaurant **: Update the Highlights at Our Restaurant content.
- **Featured Menu**: Update the Featured Menu, keep image link, change title , description , price if provided.
- **Contact and locations**: Update the Contact and locations. If there is 1 branch, remove the extra tab. If there are many branches, add the same code yourself.
Map the correct fields (if any): Name, Price, Link Map, Link Embed Map, Address, Phone number, Email, Link Social, Opening Hours to the correct branches.
If there are many branches but only 1 Opening hours data, fill in the data of the branch that has it into the branches that do not have it. 
- **Order Online Section**: Keep title, automatically generate description.Based on branch, fill links in correct format.Link to a tag and title

**Important:** Only change the content as specified. Do not alter the HTML structure, CSS, or JavaScript. Preserve all layout and style.